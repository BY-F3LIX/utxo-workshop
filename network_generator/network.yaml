setup:
    version: '3'
    services:
      Node0:
        image: node
        volumes:
          - ~/logs:/var/tmp/
        command: ["Node-0"]
      Node1:
        image: node
        volumes:
          - ~/logs:/var/tmp/
        command: ["Node-1"]
      Node2:
        image: node
        volumes:
          - ~/logs:/var/tmp/
        command: ["Node-2"]
      Node3:
        image: node
        volumes:
          - ~/logs:/var/tmp/
        command: ["Node-3"]
